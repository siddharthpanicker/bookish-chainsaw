apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "imply.fullname" . }}-test-connection"
  labels:
{{ include "imply.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "imply.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
